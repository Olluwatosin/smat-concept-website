services:
  web:
    build: .
    ports:
      - "3001:8000"
    env_file:
      - .env
    environment:
      - DEBUG=False
    volumes:
      - ./company_site/db.sqlite3:/app/db.sqlite3
    restart: unless-stopped